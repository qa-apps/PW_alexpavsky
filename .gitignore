.env
node_modules/
.DS_Store

# Playwright — all generated under test-results/ + HTML report
test-results/
playwright-report/
.playwright-mcp/
*.webm
*.zip
.last-run.json
.model-registry-cache.json

# k6 run summaries (script lives in k6/; outputs in k6/results/)
k6/results/

# Ragas / Promptfoo / eval local outputs
eval/results/
judge-verdicts/

# GitHub Pages eval site build output — only used by CI
gh-pages-site/
gh-pages-existing/

# Local agent / graph analysis artifacts
.codex/
graphify-out/

# Daily QA report UI runtime data (app code is tracked)
qa-report-ui/reports/*.json
qa-report-ui/screenshots/
qa-report-ui/server.log
qa-report-ui/shots/

# Local IDE / agent scratch
AGENTS.md
.idea/
*.log
temp_*.md
temp_*.txt
screenshot.png
.claude/settings.local.json
.claude/launch.json
scripts/*.png
scripts/files.md
tests/reg-snapshots/
/-
