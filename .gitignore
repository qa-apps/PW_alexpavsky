.env
node_modules/
.DS_Store
test-results/
playwright-report/
.playwright-report-temp/
.playwright-mcp/
chat.db
judge-verdicts/
recordings/
*.webm
*.zip
.last-run.json
.model-registry-cache.json

# Ragas evaluation artifacts (local runs)
eval/results/

# GitHub Pages eval site build output — only used by CI
gh-pages-site/
gh-pages-existing/

# Local screenshot artifacts from MCP-driven manual verification
screenshots/

# k6 performance run artifacts (CI uploads these as build artifacts)
performance-results/

# QA history / defect logs (generated, not source)
qa-history/

.codex/
graphify-out/
qa-report-ui/reports/*.json
qa-report-ui/shots/
scripts/*.png
scripts/files.md
tests/reg-snapshots/
